# 验收自测表（M1~M7）

> 生成：2026-08-21（D8）。结论一律带证据指向（命令/演示步骤编号/通检编号/文件路径），禁止三无"已完成"。
> 判定标准：通过 = 有可复现证据；部分完成 = 写明边界；未通过 = 指向差距清单原因。

| MVP | 结论 | 证据指向 | 边界/备注 |
|-----|------|---------|----------|
| **M1** 跑通官方 ng.pt 推理，README 可复现 | ✅ 通过 | `docs/演示说明.md` 步骤 1~2（serve 启动 + 10 帧评测实跑）；`README.md` 启动节；D4 首跑记录（serve 适配记录） | 他人按 README 可复现（D8 回归步骤 2 实跑 11s） |
| **M2** 单游戏 ≥500 帧统计 + ≥10 条序列可视化 | ✅ 通过 | 统计：`results/stats_summary.md`（统计集 38400 帧 > 500）；可视化：`results/figures/seq_overview.png`（10 段）+ `joystick_distribution.png` | 统计集与测试集分列（G10 已修正） |
| **M3** 离线评测脚本（≥200 帧隔离测试集） | ✅ 通过 | `results/test_set_metrics.md`（200 帧明细 + 合计）；命令 `evaluate.py --n 200 --k 3`；测试集 = chunk_0032~0034（与统计集 0~640s 隔离） | 隔离性由 pick_frames 逻辑 + 帧号范围保证 |
| **M4** zero-shot 基线：按键 ≥50%、摇杆相关 ≥0.4 | 🟡 部分完成 | 按键：`test_set_metrics.md` **88.3% ✅**；摇杆：相关系数 **-0.1 ❌**（未达 0.4） | 摇杆不达标归因见 G11：无条件 zero-shot 模型能力上限（game conditioning 无接口 + PRED_ROW 18 步全测排除）；**如实呈现，不修饰** |
| **M5** 模型输出 vs 人类标注对比演示 | ✅ 通过 | `results/m5_demo_table.md`（5 帧三对照，G1 正确键序映射）；帧缩略图 `_data/frames/Z1r1S--MJS4/seq/` | 可逐帧核对（帧号映射见 T1 验证记录） |
| **M6** 归档：代码 + 指标表 + ≥3000 字实验报告 | 🟡 D8 素材齐备，正文 D9 成稿 | 代码：scripts/ + docs/ 全量在仓；指标表：results/*.md 在仓；验收自测表/通检/排错均入仓；**实验报告正文归 D9**（结课大报告，课程节奏：报告等老师讲重点后写） | 仓库体积 3.96 MiB ≤10MB ✅ |
| **M7** 扩展：可视化工具 ≥20 段曲线 + 差异 5 帧 | ✅ 通过 | `results/figures/curves/`（**21 段**） + `index.md`（每段 top5 diff 帧 + 差值）；命令 `viz_curves.py --batch` | 21 段 ≥ 20 段达标；每段自动标 top5 + 注明差值 |

## 不做清单核对（范围防蔓延）

| 立项书"不做"条目 | 现状 |
|-----------------|------|
| 实机 Windows 游戏接入 | ✅ 未做（全程离线） |
| 模型微调 / 后训练 | ✅ 未做（仅 zero-shot 推理） |
| 数据过滤管线 | ✅ 未做 |
| Open P2P 对照实验 | ✅ 未做 |
| 下载数据集全库 | ✅ 未做（仅单视频 35 chunks） |
| 自采游戏数据 | ✅ 未做 |

## 遗留（明确不阻塞验收）

| 项 | 级别 | 说明 | 处置 |
|----|------|------|------|
| G5 演示录屏 | 加分项 | 文字版演示说明已可跟做；录屏可选 | D9 前用户按操作步骤录 |
| G7 精确抽帧 | P2 | ffmpeg `-ss` 1~2 帧 seek 误差，对 60fps 统计无影响 | 决策：不做（过度工程） |
| L1 zmq 报错文案 | P2 | evaluate 入口报原始 zmq.Again，不直观 | D8 T5 修复 |
| 报告 04~07 | 必交 | 等老师讲重点后写（用户节奏） | 素材已备齐（开发日志/通检/排错） |
