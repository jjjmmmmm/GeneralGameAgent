# 快速通检

> 讲义要求：≥5 条快速通检（含 ≥1 异常路径），逐条执行记录。任何修复后须复跑关键条目（防大范围改动弄坏已通路径）。
> 执行时间：2026-08-21（D7）| 环境：RTX 5060 + venv + serve（HF_HUB_OFFLINE 模式）

## 通检表

| # | 类型 | 步骤 | 期望 | 实际 | 通过 |
|---|------|------|------|------|------|
| C1 | 环境 | serve 启动 + 端口探活（`Test-NetConnection 127.0.0.1 -Port 5555`） | ~40s 后 5555 可连接 | 75s 就绪（首次冷启 + 本轮多进程交互），端口 True | ✅ |
| C2 | 主路径 | `evaluate.py --n 10 --k 1 --out results/test_set_metrics_smoke.md` | 13~15s 完成，输出指标，smoke 独立文件，正式报告不被覆盖 | 15s，avg_acc=87.1%、recall=5.3%，`test_set_metrics_smoke.md` 独立生成 | ✅ |
| C3 | 数据 | `stats.py` 单 chunk（chunk_0000） | 输出 17 键频率表，帧数 1200 | 1200 帧，right_trigger 32.1%（385 次）等 17 键频率表完整 | ✅ |
| C4 | 可视化 | `viz_curves.py` 单段（--start 640 --frames 10） | 生成 PNG 含 top5 标注 | seq_demo.png 生成，top5=[6,1,0,3,2]，diff 值正常 | ✅ |
| C5 | **异常** | 坏输入：① 不存在的帧路径 ② 全零图像喂 predict（`scripts/bad_input_test.py`） | ① 明确报错 ② 模型正常响应不崩溃 | ① `FileNotFoundError: No such file or directory` ✅ ② 全零图返回 shape (18,2)/(18,21) 正常 | ✅ |
| C6 | **异常** | 停 serve 后跑 `evaluate.py` | 连接错误且报错信息可读 | 抛 `zmq.error.Again: Resource temporarily unavailable`，**明确报错退出、无静默错误指标**；但报错文案不直观（见遗留） | ✅（附遗留） |

## 遗留（非阻塞）

| # | 级别 | 内容 | 计划 |
|---|------|------|------|
| L1 | P2 | C6 的 zmq.Again 报错对非技术用户不直观——evaluate.py 入口可包装为"无法连接推理服务，请先启动 serve（见演示说明步骤 1）" | D8 收尾顺手处理 |
| L2 | P2 | C2 验证时正式报告 `test_set_metrics.md` 未被覆盖（--out 生效）——快速档的默认输出保护已由演示说明约束 | 无需处理 |

## 说明

- C1 本轮耗时 75s 略高于演示说明的 40s：因 C6 前刚杀过 serve 冷启 + 批量渲染后首载，属正常波动；后续冷启实测 ~40s。
- 异常路径设计意图：坏输入**必须被拒绝**（报错即通过），若静默产出错误指标才是缺陷——C5/C6 均满足。

## D8 回归（2026-08-21）

> 目的：确认 D7 review 修复（common 抽取、MODEL_STEPS 18、probe 重构、bad_input 真实路径）未破坏任何旧路径。逐条复跑 D7 通检。

| # | 类型 | 复跑命令 | 结果 | 与 D7 差异 |
|---|------|---------|------|-----------|
| C1 | 环境 | serve 启动 + 端口探活 | ✅ 45s 就绪 | 无（D7 为 75s，本轮冷启更快） |
| C2 | 主路径 | `evaluate.py --n 10 --k 1 --out smoke` | ✅ avg_acc=89.4%, 11s | 无（D7 为 87.1%，随机性正常波动） |
| C3 | 数据 | `stats.py chunk_0000` | ✅ 17 键频率表，1200 帧 | 无 |
| C4 | 可视化 | `viz_curves.py` 单段 | ✅ top5=[1,6,2,4,9], diff=[5.08,...] | 无 |
| C5 | 异常 | `bad_input_test.py` | ✅ C5-1 FileNotFoundError / C5-2 全零图正常 | 无 |
| C6 | 异常 | 停 serve → evaluate | ✅ 报可读提示（"无法连接推理服务…请先启动 serve"） | L1 已修复：evaluate.py 入口 try/except 包装 zmq.Again |

**结论**：D7 review 修复未破坏任何既有路径，C1~C6 全部通过；L1（C6 报错文案）已在 D8 修复并经 C2 复跑确认主路径正常。
