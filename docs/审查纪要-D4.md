# 审查纪要 D4 · 全量提交通读审查

> 审查人：本人（独立完成） | 审查时间：2026-08-21 | 审查范围：仓库全部提交（D1~D4）
> 审查性质：初始脚手架合入前的全量通读（讲义 D4 要求），按五方面逐项检查

## 0. 审查范围（提交清单）

```
233cd75  Initial commit
4923c5d  docs: 第1天立项书
9e3d253  docs: project README
4dbd732  D2: module design & tech selection docs, gitignore
889f8a3  setup: AGENTS.md, agents docs, gitignore scratch
2cabf72  D3: data model & interface contracts doc, project notes, stats script
06fb61d  D3: finalize data model, interface contracts; fix RGB/zmq error specs
79049a9  D3: code review fixes - dedupe parquet read, requirements, gamemap verify script
17f2f82  D4: note yt-dlp frame fetch blocked, defer to D5
268f9e0  D4: M1 serve.py first run success - log adaptation, run_serve script, notes
```

跟踪文件 20 个：`docs/`（8 个）、`scripts/`（3 个）、`AGENTS.md`、`README.md`、`requirements.txt`、`.gitignore` 等。

## 1. 目录检查

| 检查项 | 结论 |
|--------|------|
| 语言栈/框架 | ✅ 仅 Python（requirements.txt 明确）+ Markdown 文档，无巨型模板、无错误语言栈 |
| 大文件/二进制 | ✅ 无权重（`_models/` 在外）、无数据集（`_data/` 在外）、无编译产物 |
| 目录职责清晰 | ✅ docs/（设计文档）、scripts/（可运行脚本）、.scratch/（本地 tracker，gitignore） |

## 2. 配置与密钥检查

| 检查项 | 结论 |
|--------|------|
| 密钥/Token/密码 | ✅ `grep` 无 `api_key`/`password`/`token=`/`.env`（仅文档中 `tokenizer_cfg` 等官方字段名，非密钥） |
| .gitignore | ✅ 覆盖 `.venv/`、`_models/`、`_data/`、`*.log`、`.scratch/`、`.codebuddy/` |
| 仓库地址/外链 | ✅ 无硬编码本机路径；README 引用官方资源链接（正常） |

## 3. 与 D3 约定一致性检查

| 检查项 | 结论 |
|--------|------|
| stats.py 接口 vs 数据模型文档 | ⚠️→✅ **发现不一致并已修复**：D3 code-review 将函数改名 `button_frequency`→`button_stats` 且返回结构改为 `(帧数, freq, counts)` 三元组，但 `docs/数据模型与接口约定.md` C 模块与 `docs/项目备忘.md` 接口索引仍写旧名旧签名。已同步为 `button_stats(parquet) -> tuple[int, dict, dict]`（commit 待提交） |
| verify_gamemap.py vs 3.1 结论 | ✅ 脚本输出与文档 3.1（game_mapping_cfg=None → 无条件分支直启）一致 |
| serve 适配记录 vs 实际代码 | ✅ `docs/serve适配记录.md` 第 186 行 `model.vision_model`→`model` 与 nitrogen.py 实际修改一致 |
| requirements.txt | ⚠️→✅ **发现不完整并已修复**：D3 清单缺 serve 实际运行所需的 torchvision、regex、tokenizers、safetensors、huggingface-hub、pydantic_core；已补全并注明 transformers>=4.46 与 cu128 安装方式 |

## 4. 依赖检查

| 检查项 | 结论 |
|--------|------|
| 依赖可安装 | ✅ 全部按 requirements.txt 安装成功（D4 实测 serve.py 跑通） |
| 无实机注入库 | ✅ venv 内确认无 vgamepad/dxcam/pyautogui/gymnasium（--no-deps + 显式清单规避） |
| 版本匹配 | ✅ torch 2.11.0+cu128 / torchvision 0.26.0+cu128 匹配；transformers 5.15.1 需 nitrogen 适配（已记录） |

## 5. 依赖幻觉检查

| 检查项 | 结论 |
|--------|------|
| 引用的库真实存在 | ✅ 全部 import 均实际跑通（stats.py / verify_gamemap.py / serve 推理） |
| 无虚构依赖 | ✅ 无引用不存在的第三方库 |

## 6. 已知遗留（非阻塞）

| 项 | 说明 |
|----|------|
| 取帧（yt-dlp） | YouTube bot 拦截 + GitHub 网络受限，延 D5 前解决（见 `docs/项目备忘.md`） |
| 推理输入为灰图 | D3 帧假实现；真实帧取到后替换（D4 仅验证链路） |
| run_serve.ps1 需 TUN/代理前提 | 已在脚本注释与 README 说明 |

## 7. 审查结论

- **发现并已修复 2 处**：① stats.py 接口名与文档不一致（C 模块签名/返回结构）；② requirements.txt 依赖清单不完整。
- 其余方面通过：目录干净、无密钥、约定一致（修复后）、依赖完整且无实机库、无依赖幻觉。
- 审查后代码可进入 D5 主路径贯通开发。
