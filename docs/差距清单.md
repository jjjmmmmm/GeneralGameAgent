# 差距清单

> 维护中（最新更新 2026-08-21，D6 收尾）。每条用"约束/需求/范围/输出"四要素格式（讲义要求），禁止"继续完善"式形容词。
> 优先级：P0 阻塞主路径 / P1 影响交付质量 / P2 可选增强。
> 已关闭条目保留 ✅ 记录（含结论与证据），供报告/复盘引用。

## G1 [P0] 17↔21 键位置映射严格验证 —— ✅ D6 关闭

> 源码法确定：官方 `BUTTON_ACTION_TOKENS`（shared.py）即模型 buttons 21 维顺序。结论见 `docs/键序映射表.md`。相关性法因模型预测稀疏（30 帧仅 8 按键事件）信号不足，未能二次确认——记录局限。
> **D5 的"前 17 列"假设错误**，已用正确映射重算（e2e avg acc 88.2%）。

## G2 [P0] 200 帧全量测试集评测（M3 验收 / M4 关键）—— ✅ D6 关闭

> `scripts/evaluate.py --n 200 --k 3` 完成（chunk_0032~0034，640~700s，378s）。结果见 `results/test_set_metrics.md`：按键准确率 88.3%、触发召回率 4.5%、摇杆相关系数 -0.095。M4 部分达标（见 G11）。

## G3 [P0] 真正"按键触发一致率"指标 —— ✅ D6 关闭

> evaluate.py 已输出触发精确率 54.5% / 召回率 4.5% / F1 0.083。指标口径确认：模型预测稀疏（200 帧 33 次 vs 标注 400 次），准确率 88.3% 含大量 0==0，触发指标才是真实能力反映。

## G4 [P1] 序列可视化补到 ≥10 条 —— ✅ D6 关闭

> visualize.py 起点扩至 10 段（60/180/300/420/450/480/510/540/570/600s），`results/figures/seq_overview.png` 已重生成（2 行×5 列）。M2 的"≥10 条序列"验收闭合。

## G5 [P1] 演示说明录屏版 —— 🟡 D8 用户操作（可选加分项）

- **约束**：文字版演示说明已可跟做（D8 回归自走通过）；录屏为可选加分项
- **需求**：用户按 `docs/录屏操作步骤.md` 用 OBS 录 5~10 分钟演示流程
- **输出**：`results/demo.mp4`（不入仓，体积原因）
- **计划天**：D9 前用户视时间完成；不做不影响 M1~M7 验收（M5 已由离线对比表达成）

## G6 [P1] 演示说明"自走一遍"补做 —— ✅ D6 关闭

> 演示说明 v2 完成（`docs/演示说明.md`），按新脚本 evaluate.py + 键序映射 + K=3 随机性控制更新，6 步编号齐全；**自走一遍完成**（真实从"serve 未启动"状态走查：杀残留进程 → 重启 → 10 帧快速验证 → 确认正式 200 帧报告）。发现并修复 2 个文档/环境坑：
> 1. **HF_HUB_OFFLINE=1**：serve 无代理时卡死在 config 打印后（联网检查），`_start_serve_d5.ps1` 内置离线模式，~40s 就绪；
> 2. **快速档 --out**：步骤 2 快速验证需独立输出文件，避免覆盖正式 200 帧报告。

## G7 [P2] 帧索引精确抽帧（避免 ffmpeg seek 误差）——❌ 决策不做

- **约束**：ffmpeg `-ss` 有 1~2 帧 seek 误差；对 60fps 的统计/评测（每秒抽样或隔帧）无实质影响
- **决策**（D8 T0 确认）：不做。属过度工程，不投入时间
- **依据**：评测抽帧为等间隔秒级采样（`evaluate.py` 每秒 1 帧），1~2 帧偏移在 60fps 尺度下不影响按键统计与摇杆相关性结论；精确抽帧仅对逐帧微分析有意义，而 G11 已证明摇杆能力上限与抽帧误差无关

## G8 [P2] M7 批量可视化工具 —— ✅ D7 完成（M7 达成）

- `scripts/viz_curves.py` 新增 `--batch` 模式：21 段 × 20 帧（60~660s 每 30s 一段，含测试集 630/660），每段自动标 top5 差异帧 + 差值，生成 `results/figures/curves/` 下 21 张 PNG + `index.md`（每段 top5 明细）。
- **M7 验收达成**：≥20 段（实际 21 段）、每段标出差异最大 5 帧并注明差值（index.md 有 diff values）。
- 执行：`python scripts/viz_curves.py --batch --frames 20 --k 1`（约 7 分钟，21 段）。

## G9 [P0] D5 端到端仅 5 帧（计划 T3 要求 ≥20 真帧）——✅ D6 关闭

> 5 帧链路验证不足，但 D6 已用 ≥200 帧测试集评测（G2）覆盖，本条目随 G2 关闭。

## G12 [P1] D7 代码 review 修复——✅ 已修复

- **约束**：D7 代码通过 code-review（e23488b...HEAD）后发现的重复/不一致必须收口，且修复后复跑关键脚本确认不破坏主链路
- **需求**：
  - **抽帧逻辑三处重复**（evaluate.py / viz_curves.py / probe_pred_row.py 各自内联 `fetch_frame`）→ 提取进 `scripts/common.py`
  - **常量重复**：`CHUNK_SIZE`/`VIDEO` 入 common；`MODEL_STEPS` 修正 8→18（实测），`N_STEPS=18` 魔法数字消除
  - **probe_pred_row.py docstring 漏改**（仍写"8 步"）→ 改 18 步 + 加 main() 结构
  - **bad_input_test.py**：C5-1 改为走真实 `fetch_frame` 越界路径（原用 np.load 模拟，与通检意图不符）
  - **probe 未复用 T1 现成预测**（Spec 轴指出）：本次保持独立重推理（结论已有效且批量渲染已完成，复用需改架构），如实记录偏差
- **范围**：5 个脚本（common/viz_curves/evaluate/probe_pred_row/bad_input_test）；verify_keys.py 为归档脚本保持不动
- **输出**：修复后 compile + lint 干净；bad_input（C5 两条 PASS）、probe（18 步全接近 0，G11 收口不变）、evaluate（avg_acc 89.4%）、viz_curves（top5 正常）全部复跑通过
- **计划天**：D7 完成

## G11 [P0] M4 摇杆相关系数未达标（-0.095 < 0.4）——✅ D7 归因收口（不达标事实不变）

- **结论**：200 帧测试集 M4 判定保持：按键准确率 88.3% ✅；摇杆 j_left 相关系数 -0.1 ❌。**归因验证完成（D7）**：
  1. **无条件分支（确认，且不可修复）**：读 `serve.py`/`inference_session.py`/`inference_client.py` 确认——ng.pt 的 `game_mapping_cfg=None` → serve 启动走无条件分支（打印 "No game mapping available"）；predict 请求仅含 `image`，**无 game 字段接口**。模型就是无条件 zero-shot，课程范围内无法通过现有 serve 添加 game conditioning（除非换权重/重训）。
  2. **PRED_ROW 时序（排除）**：`scripts/probe_pred_row.py` 对 30 帧测试集计算全部 **18 步**（实测模型输出 18 步动作，非 8 步）与标注 j_left 的相关系数矩阵——所有步 |corr|≤0.159，无一步显著，**不存在取错步问题**，维持 PRED_ROW=0。
  3. **模型能力上限（结论）**：综合 1+2 与 200 帧数据（pred 按键事件 33 vs gt 400、摇杆与人类无相关），根因 = **无条件 zero-shot 下模型对未见游戏的摇杆预测能力上限**，非实现缺陷。
- **验收口径**：M4"摇杆相关系数 ≥0.4"未达标，如实呈现；对应立项书 R4 缓冲已消耗（结论为能力上限而非可修缺陷），D8 报告与答辩以"能力上限 + 三假设验证证据"应对。

## G10 [P1] M2 频率表初版含测试集帧——✅ 已修正

- **约束**：M2 统计口径应只用统计集（与测试集隔离，立项书 M3）
- **需求**：初版 `stats_summary.md` 频率表基于全部 42000 帧（含测试集 chunk_0032~34）；code-review 指出后已修正为"统计集频率表（M2 口径）/ 测试集频率表（M3 口径）/ 全部合计（仅供参考）"三张分列
- **范围**：仅 `scripts/run_stats_all.py` 与 `results/stats_summary.md` 重生成
- **输出**：分列频率表 + 本记录
- **计划天**：D5 已完成

## 历史记录：D5 收尾时达成项

- ✅ M1 推理服务跑通（端口 5555，~36s 就绪）
- ✅ M2 前半：42000 帧全量统计 + 17 键频率表
- ✅ M2 后半：摇杆分布图（2×2 直方图）+ 3 条序列可视化（计划砍单剩 7 条 D6 补）
- ✅ T3 端到端贯通：5 帧真帧 → ModelClient.predict → 简化指标 85.9%
- ✅ M5 演示素材：对比表
- ✅ 演示说明（文字版）
