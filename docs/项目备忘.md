# 项目备忘

> 可持续维护的活文档。**每天开工先读本文件**；关键任务完成/新结论产生时更新。
> 更新规则：技术栈结论与接口索引只增不删（过时的标 `[过时]`）；待决问题解决后标 ✅。

## 1. 技术栈结论

| 项 | 结论 | 备注 |
|----|------|------|
| 推理接入 | 官方 `serve.py`（ZeroMQ REP :5555，pickle）+ `nitrogen.inference_client.ModelClient` | 复用官方，不开发 |
| 可视化 | matplotlib（静态 PNG） | 弃 plotly：报告嵌入/体积 |
| 数据处理 | polars | 官方同款；弃 pandas |
| 帧获取 | yt-dlp（`--download-sections` 按起止时间截段） | 选 video 源为 youtube |
| Python | 3.12 + venv（仓库内 `.venv`） | 系统级 3.12.10 |
| PyTorch | 2.11.0+cu128（Blackwell sm_120 必需） | RTX 5060 Laptop GPU，CUDA 验证通过 |
| 数据 | SHARD_0088 / Z1r1S--MJS4（rocket_league） | 35 chunks，含 actions_processed |

## 2. 主路径接口索引

> 完整定义见 `docs/数据模型与接口约定.md`。

| 模块 | 接口 | 一句话 |
|------|------|--------|
| A data_loader | `list_chunks(shard_path, game) -> list[ChunkMeta]` | 列出分片内指定游戏的 chunk 索引 |
| B frame_fetcher | `fetch_frames(chunk, out_dir) -> list[FrameRecord]` | 按 metadata 从 youtube 截帧（yt-dlp） |
| C stats | `button_frequency(parquet) -> dict[str,float]` | 17 键按下占比表 |
| D infer_client | `InferClient.predict(frame) -> ActionBlock` | ZeroMQ 走官方 serve.py 得 8 步动作块 |
| E evaluator | `run(test_frames, predictions) -> (EvalSummary, list[MetricRow])` | 按键准确率/摇杆 MSE/相关系数 |
| F viz | `render_curve(action_pairs, out_png)` | 双线对比图，≥20 段标差异帧 |
| G archive | 归档各模块输出 | 指标表 + 报告素材 |

## 3. 临时假实现边界

> 假实现只用于流程联调，**不得参与 M4 指标计算**。

| 假实现 | 固定返回 | 换真时间 |
|--------|---------|---------|
| 推理 `FakeModelClient` | buttons 全 False，j_left/j_right=(0,0)，8 步同值 | **D4** serve.py 首跑成功 |
| 帧 | 纯灰图 `np.zeros((1080,1920,3), uint8)` | **D4~D5** yt-dlp 取帧落地 |

## 4. 待决问题

| 问题 | 状态 |
|------|------|
| ~~game mapping 是否可用~~ | ✅ **D3 结论：ng.pt 的 game_mapping_cfg=None，serve.py 走无条件分支直接可启动，无需适配层** |
| vision_encoder（siglip2）首次加载需联网下载 | 未解决：D4 首跑前确保 HF/hf-mirror 可达 |
| 17↔21 键对齐 | ✅ 用官方 actions_processed（17 键已重映射），4 拨片键丢弃 |
| 测试集划分口径（≥200 帧隔离） | 未解决：D5 确定 |
| ~~yt-dlp 取帧~~ | ⚠️ **D4 前受阻**：YouTube bot 拦截（需 PO Token）；bgutil PO provider 的 generate_once 脚本获取因 GitHub 网络/路径受阻。**取帧延后 D5 前集中解决**（备选：GitHub API 列真实路径、或录屏方案）。D4 先做 serve.py 首跑（不依赖视频） |

## 5. 环境坑（持续累积）

- 工具会话拦截 pip install / 杀后台进程 → 走 `.ps1` + WMI（Invoke-CimMethod）托管 + 轮询日志
- 命令行中文路径/文件名乱码 → 一律用 UTF-8 `.py` 脚本文件操作
- git add 中文文件名失败 → 用 `git add <目录>` 或绝对路径
- HF 直连超时 → hf-mirror（下载实测可达）
- **pip extras 语法 `path[serve]` 在 WMI/cmd 里会被吞**（装成完整依赖含实机库）→ 改用显式包清单 + `-e path --no-deps`（D3 实测）
