# 开发日志

> 记录每日两块任务的计划摘要、自测命令与结果（讲义要求：计划—执行—核对三步留痕）。

## D6（2026-08-21）

### 块A：测试集评测与 M4 判定（G1/G2/G3/G11）

**计划摘要**：验证键序映射 → 200 帧隔离测试集评测 → M4 判定。

**执行记录**：
1. **G1 键序映射**：读 `NitroGen/nitrogen/shared.py` 得官方 `BUTTON_ACTION_TOKENS`（21 维按钮顺序），与标注 17 键列名对齐成映射表（`docs/键序映射表.md`）。**D5"前 17 列"假设被推翻**（south/start/west 实际在 18/19/20 列）。
   - 自测命令：`python scripts/verify_keys.py`（相关性法）——30 帧预测按下率 vs 标注按下率 Spearman rho=0.321，**信号不足**（模型预测稀疏 30 帧仅 8 键事件）。源码法为准，相关性法记录局限。
   - 自测命令：`python scripts/probe_row.py`（PRED_ROW 时序探测）——5 帧全 row/shift 命中 0，无法验证时序偏移，维持 row 0。
2. **G2 200 帧评测**：`python scripts/evaluate.py --n 200 --k 3`（后台，378s）。
   - 自测：先 `--n 10 --k 1` 冒烟（11s）通过，再全量。
   - 结果：按键准确率 **88.3%**、触发精确率 54.5%、召回率 **4.5%**、F1 0.083、j_left MSE 0.3428、相关系数 **-0.095**。总按键事件 pred=33 vs gt=400。
3. **G3 触发指标**：evaluate.py 内置输出，口径确认（准确率虚高，触发指标为真实能力）。
4. **M4 判定**（G11）：按键准确率 ✅ 达标；摇杆相关系数 ❌ 未达标。归因假设：无条件分支（无 game conditioning）、PRED_ROW 时序、模型能力。D7 处理。

**自测核对**：evaluate.py 两次运行（冒烟/全量）脚本逻辑一致；`results/test_set_metrics.md` 每帧 200 行齐全。

### 块B：序列可视化补全 + M7 骨架（G4/G8）

**计划摘要**：G4 序列补至 ≥10 条；viz_curves.py 单段骨架。

**执行记录**：
1. **G4**：`visualize.py` 起点 3→10 段（补 420/450/480/510/540/570/600s），GridSpec 3×5 → 2 行×5 列。
   - 自测：`python scripts/visualize.py` → `results/figures/seq_overview.png`（10 段横排）生成成功。
2. **M7 骨架（G8 起步）**：新建 `scripts/viz_curves.py`——单段动作曲线（上两栏：gt/pred 按键 17 键热图；下栏：j_left 实线/虚线对比 + 差异 top5 帧红点标注）。
   - 自测：`python scripts/viz_curves.py --start 640 --frames 10 --k 1` → `results/figures/curves/seq_demo.png`，top5 diff 帧输出正常。
   - 修 bug：imshow 维度错误（转置）；中文标题缺字体（改英文，DejaVu Sans 无中文字形）。
3. **批量 ≥20 段 + 差异帧标注完整版**：留 D7（G8）。

### 遗留
- G6 演示说明需按新脚本（evaluate.py/viz_curves.py）与键序映射更新并自走一遍（D6 收尾前或 D7）
- G8 M7 批量版（D7）
- G11 M4 摇杆不达标（D7）
