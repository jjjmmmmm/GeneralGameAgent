# 开发日志

> 记录每日两块任务的计划摘要、自测命令与结果（讲义要求：计划—执行—核对三步留痕）。

## D6（2026-08-21）

### 块A：测试集评测与 M4 判定（G1/G2/G3/G11）

**计划摘要**：验证键序映射 → 200 帧隔离测试集评测 → M4 判定。

**执行记录**：
1. **G1 键序映射**：读 `NitroGen/nitrogen/shared.py` 得官方 `BUTTON_ACTION_TOKENS`（21 维按钮顺序），与标注 17 键列名对齐成映射表（`docs/键序映射表.md`）。**D5"前 17 列"假设被推翻**（south/start/west 实际在 18/19/20 列）。
   - 自测命令：`python scripts/verify_keys.py`（相关性法）——30 帧预测按下率 vs 标注按下率 Spearman rho=0.321，**信号不足**（模型预测稀疏 30 帧仅 8 键事件）。源码法为准，相关性法记录局限。
   - 自测命令：`python scripts/probe_row.py`（PRED_ROW 时序探测）——5 帧全 row/shift 命中 0，无法验证时序偏移，维持 row 0。
2. **G2 200 帧评测**：`python scripts/evaluate.py --n 200 --k 3`（后台，378s）。
   - 自测：先 `--n 10 --k 1` 冒烟（11s）通过，再全量。
   - 结果：按键准确率 **88.3%**、触发精确率 54.5%、召回率 **4.5%**、F1 0.083、j_left MSE 0.3428、相关系数 **-0.095**。总按键事件 pred=33 vs gt=400。
3. **G3 触发指标**：evaluate.py 内置输出，口径确认（准确率虚高，触发指标为真实能力）。
4. **M4 判定**（G11）：按键准确率 ✅ 达标；摇杆相关系数 ❌ 未达标。归因假设：无条件分支（无 game conditioning）、PRED_ROW 时序、模型能力。D7 处理。

**自测核对**：evaluate.py 两次运行（冒烟/全量）脚本逻辑一致；`results/test_set_metrics.md` 每帧 200 行齐全。

### 块B：序列可视化补全 + M7 骨架（G4/G8）

**计划摘要**：G4 序列补至 ≥10 条；viz_curves.py 单段骨架。

**执行记录**：
1. **G4**：`visualize.py` 起点 3→10 段（补 420/450/480/510/540/570/600s），GridSpec 3×5 → 2 行×5 列。
   - 自测：`python scripts/visualize.py` → `results/figures/seq_overview.png`（10 段横排）生成成功。
2. **M7 骨架（G8 起步）**：新建 `scripts/viz_curves.py`——单段动作曲线（上两栏：gt/pred 按键 17 键热图；下栏：j_left 实线/虚线对比 + 差异 top5 帧红点标注）。
   - 自测：`python scripts/viz_curves.py --start 640 --frames 10 --k 1` → `results/figures/curves/seq_demo.png`，top5 diff 帧输出正常。
   - 修 bug：imshow 维度错误（转置）；中文标题缺字体（改英文，DejaVu Sans 无中文字形）。
3. **批量 ≥20 段 + 差异帧标注完整版**：留 D7（G8）。

### 自我复查要点（报告 06 第 4 节素材，独立完成替代交叉阅读）

针对块A/块B 的复查项：
1. **键序映射是否覆盖全部 17 键**：`BUTTON_TO_MODEL_COL` 17 个键均有映射、无遗漏；`VALID_MODEL_COLS` 与映射一致（复查过 verify_keys 输出，right_trigger 标注率 93% 对应模型列 16 按下 1/30——映射正确但模型能力弱）。
2. **随机性控制是否真生效**：K=3 多数票每次推理前 reset（单帧上下文），冒烟（K=1）与全量（K=3）脚本同逻辑；多次运行整体趋势一致（准确率 ~88%、召回率 <10%）。
3. **测试集隔离是否严格**：pick_frames 只取 chunk_0032~0034（640~700s），统计集（0~640s）未混入；frames 列表首尾帧号 38400/41940 均落在测试集内。
4. **异常路径**：ffmpeg 抽帧失败、zmq 超时会在 fetch_frame/predict 抛错并中断——评测脚本无 try/except 吞异常（宁可中断不可静默错），符合"坏输入被拒绝"要求。
5. **命名与接口一致性**：evaluate.py 复用 common 的映射/常量，未再内联重复；符合 D4 审查确立的"复用既有接口"约定。

**遗留**：
- G6 演示说明 v2 更新 + 自走（本日补做，见下方）
- G8 M7 批量版（D7）
- G11 M4 摇杆不达标（D7）

**D6 收尾（G6）**：
- 演示说明更新为 v2（改用 evaluate.py + 键序映射 + K=3 随机性控制）→ `docs/演示说明.md`
- **自走一遍完成**（真实从 serve 未启动状态）：
  - 发现**残留 serve 进程**（系统 python 34632 占着 5555）→ 清掉重起；
  - 发现 **HF 离线坑**：serve 无代理时卡死在 config 打印后（transformers 联网检查 HF，CPU=0 端口不开），`HF_HUB_OFFLINE=1` 强制离线用缓存后 ~40s 就绪 → 已内置进 `_start_serve_d5.ps1`；
  - 10 帧快速验证通过（13s）；
  - 发现**快速档覆盖正式报告**坑 → 演示说明步骤 2 加 `--out` 独立文件；
  - 因快速档覆盖了 `test_set_metrics.md`，已重跑 200 帧正式评测恢复（PID 28852 后台）。

## D7（2026-08-21）

### 块A：M7 批量可视化 + G11 归因（G8/G11）

**计划摘要**：批量 ≥20 段曲线 + 摇杆归因三假设验证 + 通检 6 条 + 排错归档。

**执行记录**：
1. **G8/M7 批量**：`viz_curves.py --batch` 21 段 × 20 帧（60~660s 每 30s）全量渲染 + `index.md`（每段 top5 + 差值）。`.gitignore` 调整允许 `results/figures/**` 入仓（M7 交付物，25 图 3.84MB）。
2. **G11 归因**：`probe_pred_row.py` 30 帧 × 18 步相关系数矩阵——全部 |corr|≤0.159，排除 PRED_ROW 时序；game conditioning 无接口（serve.py 交互式 + predict 仅 image + game_mapping_cfg=None）。结论 = 无条件 zero-shot 摇杆能力上限，M4 不达标如实呈现。
3. **通检**：`docs/快速通检.md` 6 条全过（C1~C6，含 C5/C6 异常路径）；`bad_input_test.py` 新增；遗留 2 项 P2（zmq.Again 文案、--out 保护）。
4. **排错归档**：`docs/排错记录.md` 3 案例（HF 离线卡死 / 残留 serve 冲突 / PS UTF-8 乱码）。

**自我复查要点**（报告 07 素材）：
1. M7 段数 21≥20 达标；diff 公式 = 按键 L1 + 摇杆 L1（与计划一致）；index.md 每段有 top5+差值；
2. G11 三假设：game conditioning 无接口（源码确认）/ PRED_ROW 18 步全测排除 / 能力上限——证据链完整可复述；
3. 通检 C5 异常路径：fetch_frame 越界帧号正确报 FileNotFoundError，全零图正常响应不崩溃——坏输入被拒绝，无静默错误；
4. 修复未破坏主链路：evaluate/viz_curves/probe/bad_input 重构后全部复跑通过。

**D7 收尾（code-review 修复，G12）**：
- code-review（e23488b...HEAD）发现：抽帧三处重复（evaluate/viz/probe）、MODEL_STEPS=8 与实际 18 矛盾、probe docstring 漏改、bad_input 用 np.load 模拟而非真实 fetch_frame
- 修复：`fetch_frame`/`VIDEO`/`CHUNK_SIZE` 收进 common.py；`MODEL_STEPS` 8→18；probe 加 main() + 改 docstring；bad_input C5-1 改真实越界帧路径
- 复跑验证：compile + lint 干净；C5 两条 PASS；probe 18 步全接近 0（G11 收口不变）；evaluate avg_acc 89.4%；viz_curves top5 正常
- verify_keys.py 为归档脚本，保持不动（最小改动原则）
