# nitrogen[serve] 运行依赖（D4 实测补全；不用 extras 语法，显式清单 + --no-deps 避开实机注入库）
# 安装方式（Windows venv）：
#   1. .venv\Scripts\python.exe -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   2. .venv\Scripts\python.exe -m pip install -r requirements.txt
#   3. .venv\Scripts\python.exe -m pip install -e ../NitroGen --no-deps
# 注意：NitroGen 默认依赖含 vgamepad/dxcam/pyautogui 等实机注入库（本项目不做实机，不装）

# 核心（serve 推理）
pyzmq
pyyaml
einops
pydantic
diffusers
transformers>=4.46    # serve 需 SiglipVisionModel；5.x 需适配（见 docs/serve适配记录.md）
torchvision>=0.23     # 需 cu128，与 torch 同 index 安装

# 依赖（transformers/pydantic 传递依赖，D4 实测必须显式装）
regex
tokenizers
safetensors
huggingface-hub
pydantic_core

# 数据处理与统计（M2/M3）
polars
numpy
